const map = '在JavaScript中，数组有一个map()方法 ，它通常用于映射数据到其他数据';

const number = [1, 2, 3];
const double = number.map(x => x * 2);// [2, 4, 6]

const instructions = '使用map方法，我们可以将我们的下棋动作历史映射到屏幕中按钮表示的 React 元素上， 并显示一个按钮列表以“跳转”到过去的动作。'
